---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: Tip
description: A tip suggests why a build may have failed and how to fix it.
stability: prototype
strictProperties: true
type:
- object

definitions:
  id:
    description: unique identifier of tip
    readOnly: true
    format: uuid
    type:
    - string

  content:
    description: User friendly content of a tip
    readOnly: true
    type:
    - string

  identity:
    anyOf:
    - "$ref": "/schemata/tip#/definitions/id"

links:
- description: Check whether you have starred the project.
  href: "/<%= identity(:owner, :project) %>/builds/<%= identity(:build) %>/tips"
  method: GET
  rel: self
  title: Index

properties:
  content:
    "$ref": "/schemata/tip#/definitions/content"

id: schemata/tip
